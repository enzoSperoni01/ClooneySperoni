import React from 'react';
import Alert from '@mui/material/Alert';

const NotFoundPage = () => {
    return(
        <>
            <Alert severity="error">Perdon, no se ha encontrado el sitio que buscas</Alert>
        </>
    )
};

export default NotFoundPage;